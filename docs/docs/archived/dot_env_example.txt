# XT ETF Trading System - Environment Configuration Example
# Copy this file to project root as ".env" and fill in your values

# ================================================
# XT Exchange API Keys (Required)
# ================================================
# 用于交易和净值推送
access_key=your_access_key_here
secret_key=your_secret_key_here

# ================================================
# PostgreSQL Database (Optional)
# ================================================
# 用于订单记录和净值持久化
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=xt_etf

# ================================================
# OpenTelemetry Observability (Optional)
# ================================================
# 用于指标收集和监控
ENABLE_OTEL=true
OTLP_ENDPOINT=http://localhost:4317

# ================================================
# WebSocket Configuration (Optional)
# ================================================
# 用于实时市场数据
ENABLE_WEBSOCKET=true

# ================================================
# Logging Configuration (Optional)
# ================================================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ================================================
# Security Configuration (Optional)
# ================================================
# 允许使用明文API密钥（仅用于开发/测试）
# 生产环境应设置为 false 或删除此行
# ALLOW_PLAIN_KEYS=false

# ================================================
# Usage Instructions
# ================================================
# 1. Copy this file to project root:
#    cp docs/dot_env_example.txt .env
#
# 2. Edit .env and fill in your real values:
#    nano .env  # or use your favorite editor
#
# 3. Run the system:
#    python run_net_value.py --strategy ton3l --env qa
#
# 4. Verify configuration:
#    python run_net_value.py --strategy ton3l --env qa 2>&1 | grep "API密钥"
#    # Should see: ✅ 从 .env 文件加载API密钥

# ================================================
# Security Notes
# ================================================
# - .env file is already in .gitignore, will not be committed
# - Never share your .env file or commit it to version control
# - Rotate API keys regularly (recommended: every 90 days)
# - Use encrypted APIKey.enc files for production deployments
